version: "3.9"

services:
  web:
    build: .
    container_name: flask_api
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./data:/app/data
    command: flask run --host=0.0.0.0
    restart: always
